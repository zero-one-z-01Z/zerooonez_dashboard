# Original-source provenance

These files document the source controllers and migrations used to derive the portable starter. They are **not installed** into consuming applications.

Financial defaults and hard-coded passwords were replaced with `[REDACTED_SOURCE_DEFAULT]` before this private repository snapshot was committed. Consequently these are provenance references, not byte-identical secret-bearing copies. `SHA256SUMS.json` records hashes of the redacted files.
